package coffee.amo.astromancy.core.registration;

import net.minecraft.world.level.block.SoundType;
import net.minecraftforge.common.util.ForgeSoundType;

public class SoundTypes {
    public static final SoundType JAR = new ForgeSoundType(1, 1, SoundRegistry.JAR, SoundRegistry.JAR, SoundRegistry.JAR, SoundRegistry.JAR, SoundRegistry.JAR);
}
