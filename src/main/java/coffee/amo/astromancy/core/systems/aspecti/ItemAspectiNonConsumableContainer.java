package coffee.amo.astromancy.core.systems.aspecti;

import net.minecraft.nbt.CompoundTag;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.ItemStack;
import net.minecraftforge.common.capabilities.ICapabilityProvider;
import org.jetbrains.annotations.Nullable;
/*
public class ItemAspectiNonConsumableContainer extends Item {
    protected final int capacity;
    private final ItemStack container;
    public ItemAspectiNonConsumableContainer(Properties pProperties, ItemStack container,int pCapacity) {
        super(pProperties);
        capacity = pCapacity;
        this.container = container;
    }

    @Nullable
    @Override
    public ICapabilityProvider initCapabilities(ItemStack stack, @Nullable CompoundTag nbt) {
        return new AspectiHandlerItemStack.NonConsumable(stack, container, capacity);
    }
}*/
